version: '0.1'

services:
  face_recognition:
    image: face_obfuscator
    container_name: face_obfuscator
    working_dir: /opt/face_obfuscator
    build:
      context: .
      #Uncomment this line to run the example on the GPU (requires Nvidia-Docker)
      # dockerfile: Dockerfile.gpu 
    command: python3 face_obfuscator.py
    volumes:
      - ./:/workspace
    #Uncomment this line to run the example on the GPU (requires Nvidia-Docker)
    # runtime: nvidia
